# Copy to .env for local use if your tooling loads it (optional).
# For Render/Railway, set variables in the provider dashboard instead.

# Override config path (default: config.yml). Use a mounted path if you upload config as a secret file.
# CONFIG_PATH=config.yml

# Local dev only. Render and Railway inject PORT automatically; do not set there.
# PORT=5000
